name: SonarCloud Analysis

on:
  push:
    branches:
      - main
      - develop # Maybe
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  # Job 1: Análisis del Backend (Python/Flask)
  backend-analysis:
    name: Backend Static Analysis
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Shallow clones should be disabled for better analysis

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install Backend dependencies
        working-directory: ./Backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov pylint flake8

      - name: Run Backend tests with coverage
        working-directory: ./Backend
        run: |
          pytest --cov=src --cov-report=xml:coverage.xml --cov-report=html
        continue-on-error: true

      - name: List coverage file (debug)
        run: ls -la Backend/coverage.xml || echo "Coverage file not found"

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      - name: SonarCloud Quality Gate check
        uses: SonarSource/sonarqube-quality-gate-action@master
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        continue-on-error: true  # Permite que el workflow continúe aunque falle el Quality Gate

  # Job 2: Análisis del Frontend (React/Vite)
  # frontend-analysis:
  #   name: Frontend Static Analysis
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0

  #     - name: Set up Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: "20"
  #         cache: "npm"
  #         cache-dependency-path: Frontend/package-lock.json

  #     - name: Install Frontend dependencies
  #       working-directory: ./Frontend
  #       run: npm ci

  #     - name: SonarCloud Scan - Frontend
  #       uses: SonarSource/sonarcloud-github-action@master
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #         SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  #       with:
  #           projectBaseDir: ./Frontend
  #           args: >
  #               -Dsonar.organization=integrationsonar
  #               -Dsonar.projectKey=Camilo-Benavides_Expiry-Pal-Sonar-Cloud

  # Job 3: Quality Gate Check
  # quality-gate:
  #   name: Quality Gate Check
  #   runs-on: ubuntu-latest
  #   needs: [backend-analysis] #, frontend-analysis]

  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4

  #     - name: SonarCloud Quality Gate check
  #       uses: SonarSource/sonarqube-quality-gate-action@master
  #       timeout-minutes: 5
  #       env:
  #         SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  #       continue-on-error: true

  #     - name: Download all coverage reports
  #       uses: actions/download-artifact@v4
  #       with:
  #         path: coverage-reports
